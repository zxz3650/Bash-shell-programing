# 부록. 명령어 요약

## 셸과 도움말

| 명령 | 용도 |
|---|---|
| `type NAME` | 명령 종류 확인 |
| `command -v NAME` | 실행 가능한 명령 탐색 |
| `help NAME` | Bash builtin 도움말 |
| `man NAME` | 매뉴얼 확인 |
| `echo "$?"` | 직전 종료 상태 확인 |

## 파일과 권한

| 명령 | 용도 |
|---|---|
| `pwd`, `cd`, `ls` | 경로 탐색 |
| `file`, `stat` | 파일 종류와 메타데이터 |
| `find` | 조건 기반 파일 검색 |
| `chmod`, `umask` | 권한 설정 |
| `mktemp` | 안전한 임시 파일·디렉터리 생성 |
| `sha256sum` | 해시 생성과 검증 |

## 텍스트와 데이터

| 명령 | 용도 |
|---|---|
| `grep`, `rg` | 문자열·정규식 검색 |
| `sort`, `uniq` | 정렬과 중복 집계 |
| `cut`, `paste` | 필드 추출과 결합 |
| `sed` | 스트림 치환과 선택 |
| `awk` | 필드 기반 가공과 집계 |
| `jq` | JSON 구조 처리 |

## 프로세스와 네트워크

| 명령 | 용도 |
|---|---|
| `ps`, `pgrep` | 프로세스 조사 |
| `jobs`, `wait` | 백그라운드 작업 관리 |
| `lsof` | 열린 파일과 소켓 조사 |
| `ss` | 소켓과 수신 포트 확인 |
| `ip` | 주소와 라우팅 확인 |

## 안전 작성 체크리스트

- [ ] shebang으로 Bash를 명시했다.
- [ ] 변수와 명령 치환을 기본적으로 큰따옴표로 감쌌다.
- [ ] 여러 인수는 문자열 대신 배열로 구성했다.
- [ ] 입력값과 대상 경로를 실행 전에 검증했다.
- [ ] 오류 메시지를 stderr로 출력하고 종료 상태를 구분했다.
- [ ] 임시 자원은 `mktemp`와 `trap`으로 관리했다.
- [ ] 비밀정보가 명령행, `set -x`, 로그에 노출되지 않는다.
- [ ] 파일 삭제·덮어쓰기 전에 대상 경로를 확인하고, dry-run으로 변경 내용을 미리 확인한다.
- [ ] `bash -n`, shfmt, ShellCheck, Bats 검사를 수행했다.
