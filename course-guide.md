# 학습 안내와 실습 연결표

이 과정은 Bash라는 셸 인터프리터를 통해 Shell Programming을 배우는 실행 중심 교재다. 기존 Python 교안처럼 장의 안내를 읽은 뒤 세부 절에서 개념·예제·결과 해석·실패 사례·실습·완료 기준을 확인한다. 코드를 실행하기 전에 결과를 예상하고, 실행 후 예상과 다른 이유를 설명한다.

## 학습 환경

기본 환경은 Linux 또는 WSL2 Ubuntu의 Bash다. 기본 문법과 제공 프로젝트는 Bash 3.2 이상을 지원하며 Linux 전용 명령과 Bash 4 이상 기능은 본문에 표시한다. macOS 사용자는 BSD 도구와 GNU 도구의 옵션 차이를 확인한다. 설치와 첫 실행은 [02장](02-bash-setup.md)에서 진행한다.

## 권장 학습 순서

| 장 | 핵심 질문 | 학생 산출물 | 연결 실습 |
|---|---|---|---|
| 01 보안 조사 | 어느 시스템에서 누구의 권한으로 언제 관찰했는가? | 조사 기록·사실과 가설·도구 선택 | 노트북 00·01 |
| 02 환경 | 어떤 프로그램이 코드를 실행하는가? | 환경 기록·첫 스크립트 | 노트북 00 |
| 03 문법 | 값·인수·상태는 어떻게 전달되는가? | 이벤트 분류기 | 노트북 02·03 |
| 04 파일 | 파일명과 결과를 어떻게 보존하는가? | 파일 목록·안전한 저장 | 노트북 04 |
| 05 텍스트 | 형식·빈 결과·오류를 어떻게 구분하는가? | 로그 요약 | 노트북 05 |
| 06 프로세스 | 시작·완료·종료를 어떻게 관찰하는가? | 상태 스냅샷 | 노트북 06 |
| 07 안전 설계 | 입력과 변경의 경계를 어떻게 지키는가? | 입력 검증·변경 없는 사전 확인 | 노트북 06·07 |
| 08 자동화 | 반복 실행의 인터페이스는 무엇인가? | CLI·설정·예약 설계 | 노트북 07 |
| 09 테스트 | 실패를 어떻게 재현하고 검증하는가? | 회귀 테스트 | 노트북 09 |
| 10 구조화 | 각 함수의 역할과 입출력을 어떻게 정할 것인가? | 실행 파일·라이브러리 | 노트북 10 |
| 11 병렬 | 동시에 실행해도 결과가 같은가? | 제한된 작업·순서 보존 | 노트북 11 |
| 12 종합 | 전체 요청의 성공을 어떻게 입증하는가? | 로그 도구·스냅샷 보고서 | 노트북 08, 프로젝트 A |

노트북 번호는 교안 장 번호와 별개다. 기존 노트북 경로를 유지하고 뒤에 오류 처리·모듈·병렬 실습을 추가했으므로 위 연결표 순서로 학습한다. [실습 목록](jupyter-book/labs/README.md)에서 파일과 Colab 링크를 찾는다.

## 01장 보안 조사 실습

[01장](01-bash-intro.md)은 첫 조사 정보 → 실행 모델 → 흔적과 판단 → 조사 기록 실습의 네 상세 절로 구성한다. [노트북 01](jupyter-book/labs/01-when-to-use-bash.ipynb)에는 고정 자료·자기 환경 관찰·기대 결과·실패 검사·분석 질문이 함께 있다. 기준 구현과 검증 범위는 [첫 조사 예제](examples/first-observation/README.md)에 있다.

01장 실습의 관찰 시각은 KST(UTC+09:00)로 표시한다. 다른 실습에서 UTC로 제공되는 원본 자료는 시간대 표기를 확인하고, KST로 변환한 분석 결과와 원본 시각을 구분해 보존한다.

## 한 절을 공부하는 방법

1. 선행 지식과 학습 전 질문에 답한다.
2. 예제의 입력·출력·파일 변경 범위를 표시한다.
3. 작은 코드 블록을 실행하고 stdout·stderr·상태를 기록한다.
4. 빈 값·공백·없는 파일·잘못된 행 등 한 조건을 바꿔 실행한다.
5. 직접 해보기 문제를 구현한 뒤 해설과 비교한다.
6. 완료 기준을 통과한 결과와 설명을 함께 남긴다.

## 코드 블록 실행 규칙

- bash 블록은 Bash 터미널에서 실행한다. `#!/usr/bin/env bash`로 시작하는 완성 스크립트는 지정된 파일에 저장한 뒤 `bash 파일명`으로 실행한다.
- JSON·text·cron 블록은 데이터 또는 설정 예시다. 터미널에 명령으로 붙여넣지 않는다.
- “같은 실습에서 이어서 실행”하는 블록은 앞에서 만든 lab_dir 등을 사용한다. 별도 터미널이나 노트북의 새 Bash 셀에는 셸 변수가 이어지지 않는다.
- `%%bash`는 Python 커널의 셀 매직이다. 노트북에서는 매 셀마다 새 Bash가 시작된다. Python의 환경 변수로 전달한 BASH_LAB_DIR와 디스크 파일을 통해 상태를 공유한다.
- 설치·예약 설정은 설명된 플랫폼에서만 적용한다. 기본 실습은 관리자 권한이 필요하지 않다.

## 결과 기록 양식

| 입력 또는 변경 | 예상 stdout | 예상 상태 | 실제 결과 | 설명 |
|---|---|---:|---|---|
| 정상 샘플 | 지정된 TSV | 0 | 학생 작성 | 일치 여부 |
| 없는 입력 | 없음 | 1 | 학생 작성 | 오류 원인 |
| 빈 인수 | 사용법 또는 거부 | 요구사항에 따라 결정 | 학생 작성 | 빈 값과 누락 구분 |

시스템 PID·시각·호스트명은 고정 정답이 아니다. 구조와 의미를 검증한다. 합성 로그 집계와 인수 개수처럼 고정 입력의 정답은 정확히 비교한다.

## 제공 코드와 학생 과제

[로그 보고서 프로젝트](12-capstone/12-1-log-report-project.md)는 요구사항·정답 파일·기준 구현·동작 검사를 제공한다. 먼저 요구사항대로 구현한 뒤 기준 구현과 비교한다. [로컬 스냅샷 프로젝트](12-capstone/12-2-snapshot-assessment.md)는 노트북의 기본 수집기를 확장하는 과제이며 manifest·선택 항목 상태 등은 학생이 구현한다.

## 최종 제출물

소스 코드, 실행 환경, 입력 샘플, 기대 결과, 테스트 결과, 설계 이유를 제출한다. 개인 호스트·사용자 정보가 포함된 결과는 검토 후 공유한다. 평가 기준은 [12-2 루브릭](12-capstone/12-2-snapshot-assessment.md)에 있다.
