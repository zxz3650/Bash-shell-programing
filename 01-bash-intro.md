# 01. Bash와 Shell Programming 소개

**[01장 학습용 노트북 ZIP 받기](https://github.com/zxz3650/Bash-shell-programing/raw/refs/heads/master/downloads/chapter-01.zip)** · [처음 실행하는 방법](PRACTICE.md) · [포함 노트북과 자료 버전](downloads/README.md)

압축을 푼 뒤 `START-HERE.md`의 순서대로 기본 실습부터 실행하고 보안 적용 실습으로 이어갑니다.

## 개요

터미널에서 매번 입력하던 명령을 파일에 저장하고, 조건문과 반복문을 더하면 같은 작업을 자동으로 수행할 수 있습니다. 이를 **Shell Programming(셸 프로그래밍)**이라고 합니다. 이 교재에서는 명령과 스크립트를 해석하고 실행하는 셸로 **Bash**를 사용합니다.

아래 기본 설명에서 Shell·Bash·터미널의 관계와 도구 선택을 익힙니다. 이어지는 상세 절에서는 자기 실습 환경의 기본 정보를 확인하고, 명령의 결과·오류·종료 상태를 구분하여 첫 조사 기록을 만듭니다. 기본 설명과 보안 조사 실습을 함께 학습합니다.

## 상세 학습 순서

| 순서 | 보안 질문 | 실습 산출물 |
|---|---|---|
| [01-1. 첫 조사에서 확인할 정보](01-bash-intro/01-1-first-observation.md) | 현재 관찰의 시스템·사용자·범위는 무엇인가? | 호스트·권한·셸 정보 해석표 |
| [01-2. 명령이 실행되는 과정](01-bash-intro/01-2-shell-execution.md) | 입력한 문자열은 어떻게 인수·출력·상태가 되는가? | 인용·환경·실패 비교 기록 |
| [01-3. 행위·흔적·판단의 구분](01-bash-intro/01-3-artifacts-and-reasoning.md) | 명령의 흔적만으로 무엇을 결론 낼 수 있는가? | 사실·가설·추가 확인 표 |
| [01-4. 첫 조사 기록 실습](01-bash-intro/01-4-observation-lab.md) | 같은 입력에서 같은 결과를 얻고 실패도 기록할 수 있는가? | 고정 자료 분석·자기 환경 관찰·검증 결과 |

먼저 아래 용어를 읽고 상세 절을 순서대로 진행합니다. 설치가 필요하면 [02장 환경 준비](02-bash-setup.md)를 먼저 참고합니다. 기존 [노트북 01](jupyter-book/labs/01-when-to-use-bash.ipynb)은 경로를 유지하면서 이 장의 조사 실습으로 보강했습니다. 외부 접속·권한 변경·전체 시스템 검색은 수행하지 않습니다.

{% hint style="info" %}
## 🧭 학습 목표

- 셸, 셸 명령 언어, Shell Programming, Bash와 터미널의 차이를 설명한다.
- `sh`와 Bash 전용 스크립트의 실행 조건을 구분한다.
- Bash가 적합한 작업과 Python 등 다른 언어로 전환할 기준을 정한다.
- 시스템·사용자·시각·실행 범위를 함께 기록한다.
- 인용·명령 치환·표준 스트림·종료 상태를 실제 관찰에 적용한다.
- 공격자와 분석가가 같은 정보를 확인하는 이유를 비교한다.
- 관찰한 사실과 침해 여부에 대한 가설을 구분한다.
{% endhint %}

---

## 1.1 먼저 구분하는 핵심 용어

| 용어 | 정확한 의미 |
|---|---|
| **Shell(셸)** | 사용자가 입력한 명령이나 스크립트를 읽고 해석하여 프로그램을 실행하는 **프로그램** |
| **Shell Command Language** | 명령, 변수, 파이프, 리다이렉션, 조건문, 반복문, 함수 등을 표현하는 **명령 언어** |
| **Shell Programming** | 셸 명령 언어와 운영체제 도구를 이용해 작업을 자동화하는 **프로그래밍 방식** |
| **Shell Script** | 셸이 순서대로 읽고 실행할 명령을 저장한 **텍스트 파일** |
| **`sh`** | POSIX 계열 셸 명령 언어의 이식 가능한 인터페이스를 가리키는 이름. 시스템에 따라 실제 구현은 `dash`, Bash 등이 될 수 있음 |
| **Bash** | GNU가 제공하는 구체적인 **셸 프로그램이자 명령 언어 인터프리터**. `sh`와 높은 호환성을 가지며 배열, `[[ ... ]]` 등 확장 문법도 제공 |
| **Terminal(터미널)** | 키 입력과 화면 출력을 셸에 전달하는 창 또는 프로그램. 명령을 해석하는 셸 자체와는 다름 |

### Shell Programming과 Bash의 관계

```text
Shell Programming ── 셸 명령 언어로 자동화하는 활동
        │
        ├── POSIX sh 문법 ── dash, Bash, ksh 등이 해석 가능
        │
        └── 구현별 확장 문법
                ├── Bash 확장: 배열, [[ ... ]], brace expansion 등
                ├── zsh 확장
                └── fish 고유 문법
```

{% hint style="info" %}
## 셸, 문법, 스크립트의 관계

**Bash는 명령을 읽고 실행하는 셸 프로그램입니다.** 사용자는 Bash가 이해하는 문법으로 명령을 작성합니다. 이 명령들을 텍스트 파일에 저장하면 Bash로 실행할 수 있는 셸 스크립트가 됩니다.

예를 들어 `bash report.sh`에서 `bash`는 실행을 담당하는 프로그램이고, `report.sh`는 실행할 명령이 담긴 파일입니다. 이 교재에서 말하는 “Bash 문법”은 Bash가 해석할 수 있는 변수, 조건문, 반복문 등의 작성 규칙을 뜻합니다.
{% endhint %}

이 구분은 [GNU Bash 매뉴얼의 Bash 정의](https://www.gnu.org/software/bash/manual/html_node/What-is-Bash_003f.html), [GNU의 셸 정의](https://www.gnu.org/software/bash/manual/html_node/What-is-a-shell_003f.html), [POSIX Shell Command Language](https://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html)를 기준으로 합니다.

### 어떤 인터프리터로 실행할지 선언하기

다음 shebang은 이식 가능한 `sh` 문법으로 작성했다는 뜻입니다.

```sh
#!/bin/sh

printf '%s\n' 'POSIX sh script'
```

이 스크립트에서는 Bash 배열이나 `[[ ... ]]` 같은 전용 문법을 사용하면 안 됩니다. 반대로 Bash 확장 문법을 사용할 때는 Bash를 명시합니다.

```bash
#!/usr/bin/env bash

items=("alpha" "two words")
[[ ${#items[@]} -eq 2 ]] && printf '%s\n' "${items[@]}"
```

파일 확장자 `.sh`가 인터프리터를 결정하는 것은 아닙니다. 직접 실행할 때는 shebang이, `bash script.sh`처럼 실행할 때는 앞에 지정한 프로그램이 인터프리터를 결정합니다.

### 대표적인 셸 구현

| 이름 | 주된 성격 | 학습 시 주의점 |
|---|---|---|
| `dash` | 작고 빠른 POSIX 계열 셸. 일부 Linux에서 `/bin/sh`로 사용 | Bash 배열과 `[[ ... ]]`을 지원하지 않음 |
| Bash | `sh` 호환성과 실용적인 확장 기능을 함께 제공 | Bash 전용 기능을 쓰면 shebang에 Bash를 명시 |
| `ksh` | KornShell 계열 셸 | 구현별 기능 차이를 확인 |
| `zsh` | 강력한 대화형 기능과 자체 확장을 제공 | Bash 스크립트와 완전히 같지 않음 |
| `fish` | 사용 편의성을 중시한 고유 문법의 셸 | POSIX `sh` 호환 셸이 아님 |

현재 환경에서 셸과 Bash를 확인합니다.

```bash
printf 'login shell=%s\n' "$SHELL"
ps -p "$$" -o pid,ppid,comm,args
command -v sh bash
bash --version
```

`$SHELL`은 로그인 셸 설정을 보여주며, 현재 스크립트를 실제로 실행 중인 프로세스와 항상 같지는 않습니다. 현재 프로세스는 `ps -p "$$"` 결과와 shebang을 함께 확인해야 합니다.

## 1.2 왜 Bash를 배우는가

Bash는 GNU/Linux에서 널리 쓰이는 대표적인 셸입니다. Linux 서버를 관리하다 보면 파일을 찾고, 로그를 확인하고, 여러 명령을 순서대로 실행하는 일을 자주 하게 됩니다. **Bash를 배우면 터미널에서 사용하던 명령을 스크립트로 묶어 반복 작업을 자동화할 수 있습니다.**

- **익숙한 명령을 재사용합니다.** `find`로 파일을 찾고 `grep`으로 필요한 줄을 골라내는 작업을 하나의 스크립트에 담을 수 있습니다.
- **반복과 누락을 줄입니다.** 매일 같은 순서로 수행하는 로그 집계, 백업, 시스템 점검을 자동으로 실행할 수 있습니다.
- **실행 절차를 공유합니다.** 명령과 옵션을 파일로 남기면 다른 사람도 같은 절차를 확인하고 다시 실행할 수 있습니다.
- **작업 결과에 따라 다음 동작을 결정합니다.** 파일이 있을 때만 처리하거나, 명령이 실패하면 오류를 알리고 중단하도록 만들 수 있습니다.

{% hint style="info" %}
Bash가 모든 환경에 설치되어 있는 것은 아닙니다. 일부 경량 컨테이너나 복구 환경에는 `sh`만 있을 수 있습니다. 실습 전에는 `command -v bash`와 `bash --version`으로 설치 여부와 버전을 확인합니다.
{% endhint %}

## 1.3 Bash를 사용하기 좋은 상황

Bash는 운영체제에 설치된 명령을 연결해 파일과 텍스트를 처리하는 작업에 적합합니다. 다음 조건을 확인하면 Bash로 작성하기 좋은 작업인지 판단할 수 있습니다.

1. 작업의 중심이 `find`, `grep`, `awk`, `sed`, `curl`, `ssh`, `tar` 같은 외부 명령 실행이다.
2. 입력과 출력이 파일, 표준 입력·출력, 단순한 행 단위 텍스트다.
3. 처리 순서가 단순하고, 복잡한 데이터 구조나 상태 관리가 필요하지 않다.
4. 실행 환경에 Bash와 작업에 필요한 명령이 설치되어 있다.
5. 운영자가 명령을 한 줄씩 검토하고 즉시 수정할 수 있어야 한다.
6. 각 명령의 성공·실패를 확인하면서 다음 단계를 진행하는 작업이다.

| 상황 | Bash가 적합한 이유 | 대표 산출물 |
|---|---|---|
| 여러 로그에서 특정 문자열 찾기 | 파일 탐색과 텍스트 필터를 파이프로 직접 연결 | IOC 검색 결과 |
| 서버 상태 스냅샷 수집 | `ps`, `df`, `uname` 등 OS 명령을 순서대로 실행 | 점검 보고서 |
| 백업 파일 묶기와 해시 생성 | 파일 목록, 압축, 해시 명령의 조합이 핵심 | 압축 파일과 체크섬 |
| CI 품질 검사 | 각 도구의 종료 상태로 성공·실패를 판단 | 빌드 결과 |
| 승인된 랩에서 반복 명령 실행 | 동일한 명령과 옵션을 재현 가능하게 기록 | 실행 로그 |
| 컨테이너 시작 전 환경 점검 | 작은 의존성으로 빠르게 실행 | 시작 전 검증 결과 |

### 실무 예시: 여러 로그에서 오류 개수 집계

```bash
find ./logs -type f -name '*.log' -print0 \
  | xargs -0 grep -h 'ERROR' \
  | sort \
  | uniq -c \
  | sort -nr
```

이 작업은 파일 찾기, 문자열 필터링, 정렬, 집계를 기존 명령으로 해결합니다. 데이터 모델이나 복잡한 상태 관리가 없으므로 Bash의 장점이 잘 드러납니다.

## 1.4 Bash와 Python 선택 기준

| 판단 질문 | Bash 쪽 | Python 쪽 |
|---|---|---|
| 핵심 작업은 무엇인가? | 외부 명령 조합 | 자체 로직과 데이터 처리 |
| 데이터 형태는 무엇인가? | 행 단위 텍스트, 파일 경로 | JSON, 중첩 객체, 표, 바이너리 |
| 오류 처리는 얼마나 복잡한가? | 종료 상태와 짧은 분기 | 예외 유형별 복구와 재시도 |
| 코드 규모는 어느 정도인가? | 짧은 작업과 실행 래퍼 | 여러 모듈과 장기 유지보수 |
| 테스트 요구는 무엇인가? | 명령 결과와 종료 상태 확인 | 단위·통합 테스트와 모킹 |
| 동시성과 네트워크 처리는? | 제한된 병렬 명령 실행 | 비동기 I/O와 복잡한 프로토콜 |

### 빠른 결정 규칙

```text
외부 명령을 연결하는 작업인가?
├─ 예 → 데이터가 단순한 텍스트이고 흐름이 짧은가?
│       ├─ 예 → Bash 우선 검토
│       └─ 아니오 → Bash는 실행 래퍼, 핵심 로직은 Python
└─ 아니오 → Python·Go 등 범용 언어 우선 검토
```

### Bash를 사용하지 않을 기준

- 복잡한 자료구조와 비즈니스 로직이 필요하다.
- 신뢰할 수 없는 데이터를 대규모로 파싱한다.
- 바이너리 프로토콜, 암호 구현, 장시간 서비스가 필요하다.
- 세밀한 예외 복구, 타입 검증, 테스트 격리가 필요하다.
- 여러 개발자가 장기간 유지하는 대규모 코드가 된다.

이 경우 Bash는 실행 래퍼로 한정하고 핵심 로직은 Python, Go 등으로 옮깁니다.

## 1.5 보안 실무에서 Bash가 유용한 이유

| 관점 | 활용 예 |
|---|---|
| 블루팀 | 로그 필터링, 프로세스 스냅샷, IOC 검색, 증거 해시 |
| 레드팀 | 승인된 랩의 반복 검증, 도구 실행 조정, 결과 정리 |
| 공통 | 환경 점검, 파일 변환, 배치 실행, CI 품질 검사 |

중요한 기준은 공격·방어 구분보다 **허가된 범위, 재현성, 실패 시 안전성**입니다. 같은 `curl`이나 `grep` 명령도 대상과 목적에 따라 정상적인 점검이 될 수도 있고 허가되지 않은 행위가 될 수도 있습니다.

## 1.6 미니 실습: Bash가 맞는 작업 고르기

다음 과제에 Bash 또는 Python 중 하나를 선택하고 이유를 설명합니다.

1. 디렉터리 안의 `.log` 파일에서 `FAILED` 행만 모아 날짜순으로 정렬한다.
2. 중첩 JSON 이벤트를 스키마로 검증하고 오류 유형별로 재처리한다.
3. 배포 전에 세 개의 검사 도구를 순서대로 실행하고 하나라도 실패하면 중단한다.
4. 장시간 실행되는 비동기 API 서버를 만든다.

권장 판단:

- 1과 3은 외부 명령·텍스트·종료 상태가 중심이므로 Bash가 적합합니다.
- 2와 4는 복잡한 데이터 모델과 예외·비동기 처리가 필요하므로 Python이 적합합니다.

직접 실행하는 실습은 [노트북 01. Linux 보안 조사와 Shell 실행 모델](jupyter-book/labs/01-when-to-use-bash.ipynb)에서 이어집니다. 기존 도구 선택 연습도 이 노트북의 후반에 유지했습니다.

## 1.7 이 장에서 다음 장으로

```mermaid
flowchart LR
    A[조사 질문] --> B[시스템·사용자·시각 확인]
    B --> C[명령·출력·종료 상태]
    C --> D[사실과 가설 구분]
    D --> E[첫 조사 기록·검증]
```

{% hint style="danger" %}
## ⚠️ 실습 윤리

모든 보안 실습은 본인 소유 시스템, 서면 승인을 받은 대상, 폐쇄된 CTF·랩 환경에서만 수행합니다. 외부 대상에 대한 무단 스캔·접속·명령 실행은 금지합니다.
{% endhint %}

## ✅ 완료 기준

- Bash가 적합한 작업 세 가지와 부적합한 작업 세 가지를 설명할 수 있다.
- Shell Programming, 셸 명령 언어와 Bash 인터프리터의 관계를 설명할 수 있다.
- `#!/bin/sh`와 `#!/usr/bin/env bash` 중 어느 것을 선택해야 하는지 판단할 수 있다.
- 같은 과제를 Bash와 Python 중 어느 쪽으로 구현할지 판단 근거를 제시할 수 있다.
- 현재 실행 중인 셸의 PID, 부모 PID와 버전을 확인할 수 있다.
- 본 과정의 실습 범위와 금지 범위를 구분할 수 있다.
- 같은 명령의 정상 관리 용도와 공격자 관점의 의미를 구분할 수 있다.
- 수집 시각을 KST와 UTC 오프셋으로 기록하고 원본 시각을 임의로 바꾸지 않는다.
- 명령 실패를 “이상 없음”으로 기록하지 않는다.
- [01-4 실습](01-bash-intro/01-4-observation-lab.md)의 분석 질문에 증거를 붙여 답한다.

다음 학습: [01-1. 첫 조사에서 확인할 정보](01-bash-intro/01-1-first-observation.md)
