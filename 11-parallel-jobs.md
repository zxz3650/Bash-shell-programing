# 11. 웹 로그 분석과 제한된 배치 처리

## 보안 질문에서 시작하기

[웹 로그 실습](11-parallel-jobs/11-3-web-log-analysis.md)에서 요청·상태·주소·시간을 분석한 뒤 두 로컬 파일을 제한된 작업으로 처리합니다. 아래 병렬 심화는 선택 확장입니다. 명령을 빠르게 실행하는 것보다 자료 형식·실패·결과 순서를 검증하는 것이 우선입니다.

## 개요

백그라운드 작업, `wait`, `xargs -P`를 사용해 독립 작업을 제한된 동시성으로 처리하고 결과와 실패를 회수합니다.

## 학습 순서

1. [11-1. 제한된 동시성과 결과 회수](11-parallel-jobs/11-1-bounded-workers.md)
2. [11-2. 결정적 출력과 성능 측정](11-parallel-jobs/11-2-determinism-performance.md)

작업 시작과 완료, 입력 순서와 완료 순서, 동시성 증가와 실제 성능을 구분합니다. [노트북 11](jupyter-book/labs/11-bounded-parallel.ipynb)과 `examples/parallel/run-workers.sh`가 제한된 작업 실행의 기준 예제입니다.

{% hint style="warning" %}
병렬화 전에 직렬 버전의 정확성과 멱등성을 먼저 검증합니다. 여러 작업이 같은 파일을 수정하면 결과 순서와 데이터가 불안정해질 수 있습니다.
{% endhint %}

## 🧭 학습 목표

- PID와 종료 상태를 추적한다.
- 동시 실행 수를 제한한다.
- 공백과 개행이 포함된 파일명을 안전하게 전달한다.

## 1. 백그라운드 작업 회수

아래는 소수 입력에서 PID 회수만 설명하는 GNU/Linux 조각 예제이며 입력 수만큼 작업을 시작합니다. 대량 입력에는 상세 절의 동시성 제한 구현을 사용합니다.

```bash
pids=()

for file in "$@"; do
    sha256sum -- "$file" &
    pids+=("$!")
done

failed=0
for pid in "${pids[@]}"; do
    wait "$pid" || failed=1
done

exit "$failed"
```

## 2. `xargs -P`

```bash
find ./samples -type f -print0 |
    xargs -0 -r -n 1 -P 4 sha256sum --
```

`-print0`와 `-0`을 함께 사용해 파일명 경계를 보존합니다. CPU, 디스크와 대상 서비스에 과도한 부하를 주지 않도록 동시 실행 수를 작게 시작합니다.

## 3. 결과 순서와 임시 파일

병렬 작업의 완료 순서는 입력 순서와 다를 수 있습니다. 순서가 필요하면 각 작업이 별도 파일에 쓰게 한 뒤 마지막 단계에서 정렬·병합합니다.

## 🧪 종합 실습

샘플 디렉터리의 해시를 동시 실행 수 1과 4로 생성해 결과와 시간을 비교합니다. 실패 파일 수와 전체 종료 상태를 함께 보고합니다.

## ✅ 완료 기준

- 모든 자식 프로세스의 종료 상태를 회수한다.
- 동시 실행 수를 옵션으로 제한한다.
- 공유 출력 파일의 경쟁 상태를 피한다.
